# Common configuration shared between all applications
configserver:
  name: Docker JHipster Registry 01
  status: Connected to the JHipster Registry running in Docker 01

jhipster:
  security: 
    authentication:
      jwt:
        secret: U3ByaW5nIENsb3VkIENvbmZpZ3VyYXRpb24=

eureka:
  client:
    service-url:
      defaultZone: http://admin:admin@jhipster-registry:8761/eureka/
